import { SkillsPipePipe } from './skills-pipe.pipe';

describe('SkillsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
