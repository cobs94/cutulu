import { ZeroReplacePipe } from './zero-replace.pipe';

describe('ZeroReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
