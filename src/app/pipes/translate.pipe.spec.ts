import { TranslateCharacteristicsPipe } from './translate.pipe';

describe('TranslateCharacteristicsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateCharacteristicsPipe();
    expect(pipe).toBeTruthy();
  });
});
